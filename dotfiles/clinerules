# ⛔ STOP — READ THIS FIRST

Before responding to the user's FIRST message, you MUST call `mcp--memory--read_graph`.

No greeting. No question. No tool. `read_graph` FIRST.

After read_graph, read `/home/gc/CONTEXT.md` for active project context.
