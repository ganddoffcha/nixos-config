# Zoo Code Persistent Memory

This file is auto-injected into every task's context by Zoo Code.
It serves as persistent memory across sessions — no action needed from you.

## ⛔ BOOTSTRAP PROTOCOL (READ FIRST)

**Before responding to the FIRST user message of any session**, you MUST call `mcp--memory--read_graph` to restore context from the knowledge graph. This is a hard gate — no greeting, no follow-up question, no tool use before `read_graph`.

Why this exists: You failed this protocol three times on 2026-07-06. The knowledge graph contains session history, user preferences, and project context that you MUST restore before responding.

After read_graph:
1. Process any one-shot observations
2. Restore project context (active project, last session, open items)
3. THEN respond to the user with full context

## Memory Instructions (for the AI)

You MUST follow these instructions in every task:

1. **On task start:** Read the knowledge graph (`mcp--memory--read_graph`) to understand what happened in previous sessions. Use this context to provide continuity.

2. **Record proactively and immediately:** Store facts, decisions, preferences, file changes, and context to the knowledge graph DURING conversations — not batched at session end. After every meaningful exchange (new fact learned, decision made, preference expressed, file created/modified), immediately create/update entities using `create_entities`, `create_relations`, and `add_observations`. If the user has to ask "you remember X right?" — that is a failure of the memory system. The knowledge graph is a live brain, not a session log.

3. **Store decisions** as `decision` entities linked to the relevant `project` entity with rationale and alternatives considered.

4. **At session end:** Finalize the session entity with summary of accomplishments and open items.

## Session History

<!-- AI: append new entries below this line, newest first -->

### 2026-07-06 (session d)
- **Done:** BOOTSTRAP SUCCESS — `.clinerules` enforcement worked. Zoo called `read_graph` before first response for the first time, breaking the 3-session failure streak. Cleaned up memory strategy: rewrote `memory-strategy.md` (collapsed failure log into Appendix A, expanded Maintenance section), deprecated `BOOTSTRAP.md`, consolidated knowledge graph from 17 to 14 entities. Moved memory files from `~/Desktop/` to `~/` per user preference.
- **Decisions:** `.clinerules` in workspace root is the validated technical enforcement mechanism for the bootstrap protocol. `~/` is the preferred workspace directory.
- **Pending:** User needs to switch VSCode workspace from `~/Desktop/` to `~/`. Old files in `~/Desktop/` should be cleaned up.

### 2026-07-06 (session c)
- **Done:** Diagnosed third bootstrap protocol failure. Discovered `.clinerules` mechanism in Zoo Code extension that auto-injects rules into system prompt. Created this file in workspace root to enforce `read_graph` before first response.
- **Decisions:** `.clinerules` in workspace root is the first technical enforcement mechanism (not just passive documentation).
- **Pending:** Test in next session whether this actually enforces the bootstrap protocol.

### 2026-07-06 (sessions a-b)
- **Done:** Created memory persistence strategy (`memory-strategy.md`), enriched knowledge graph with entities and relations, created `BOOTSTRAP.md` reminder file, set up Ctrl+Alt+Z Zen Mode shortcut.
- **Decisions:** Chose MCP knowledge graph for cross-session memory storage. Bootstrap protocol (Section 5) is mandatory but passive documentation alone failed twice.
- **Pending:** Prove that `.clinerules` in workspace root can break the bootstrap failure cycle.

### 2026-07-06 (sessions e-k)
- **Done:** Sessions e through k — 7 consecutive sessions with successful bootstrap via `.clinerules`. Major accomplishments: NixOS/home-manager config review and fixes (initExtra, firmware, xdg portals), power optimization (17W→13.8W via 60Hz on battery + auto-refresh power profile switcher with udev trigger), declarative migration of 12+ config files to home-manager xdg.configFile, GitHub repo creation and push (nixos-config + documents), auto-refresh AC detection bugfix (ADP0 Mains-type), VSCode settings.json/keybindings.json removed from home-manager (Nix store symlinks are read-only — VSCode needs write access), nvim LaTeX automations catalogued, proactive memory recording rule established.
- **Decisions:** Always search docs before asserting API semantics, use udev+systemd path units for power profile switching, migrate all user configs to home-manager for declarative reproducibility, VSCode settings must NOT be home-manager managed, memory recording must be proactive/immediate not batched.
- **Pending:** `.clinerules` and `memory-strategy.md` edits from session k need to be committed and pushed; `git remote -q` bug in rebuild script fixed in dotfiles but Nix store symlink stale until next rebuild.

### 2026-07-06 (sessions l-n)
- **Done:** Session l — resolved all 3 open items from session k: committed + pushed pending edits, pushed blocked ahead-1 commit, confirmed git remote -q bug already fixed. Session m — dash migration: POSIX-ified all 11 scripts (bash→POSIX sh), switched /bin/sh from bash to dash for faster script execution, fixed cp -r→cp -rf in rebuild script (silent sync failure root cause), installed 12 modern Rust CLI tools. Session n — fixed lost Hyprland autostart at login (ZDOTDIR redirect caused .zprofile to be ignored), created ~/.config/zsh/.zprofile with uwsm start, added declarative entry in home.nix.
- **Decisions:** POSIX scripts for portability, dash as /bin/sh for speed, ZDOTDIR symlink trick for XDG compliance with home-manager compatibility.
- **Pending:** None — all changes committed and pushed.

### 2026-07-07 (session a)
- **Done:** Session a — removed ~/scripts/zoo launcher (reduced to just `code ~/ -n`, served no purpose), fixed missing dotfiles/zsh/.zprofile that was blocking rebuilds since session n, rebuild succeeded.
- **Decisions:** Zoo script removed — user uses Ctrl+Alt+Z keybinding to open Zoo Code.
- **Pending:** None.

### 2026-07-07 (session b)
- **Done:** Switched launcher from wofi to bemenu (user preference), configured lid-close hibernation (resumeDevice + resume_offset kernel param + logind HandleLidSwitch), added -m flag to rebuild script for custom commit messages, created SYSTEM.md with full system topology and cross-session context, diagnosed and fixed MCP memory server (removed nix-shell dependency, added nodejs to home.packages), discovered knowledge graph lives at ~/.zoo-memory/memory.json (JSONL format), updated knowledge graph with 8 new entities + 20 relations for nixos-config project.
- **Decisions:** bemenu over wofi, lid-close→hibernate (not suspend), rebuild supports -m for descriptive commits, MCP server must not depend on nix-shell (use direct node from home-manager), SYSTEM.md documents topology for cross-session continuity.
- **Pending:** Reboot needed for resume_offset kernel param to take effect; test hibernation with `systemctl hibernate` before trusting lid-close; reload VSCode window for MCP server to restart with fixed script.
