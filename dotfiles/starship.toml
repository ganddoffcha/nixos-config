# Starship prompt — minimal and colorful
# https://starship.rs/config/
"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](fg:#DA627D)\
$directory\
[](fg:#DA627D bg:#FCA17D)\
$git_branch\
$git_status\
[](fg:#FCA17D)\
$fill\
[](fg:#86BBD8)\
$nix_shell\
$cmd_duration\
[](fg:#86BBD8)\
"""

add_newline = true

[directory]
style = "bg:#DA627D"
format = "[ $path ]($style)"
truncation_length = 2
truncation_symbol = "…/"
truncate_to_repo = true

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "

[git_branch]
symbol = ""
style = "bg:#FCA17D"
format = '[$symbol $branch]($style)'

[git_status]
style = "bg:#FCA17D"
format = '[$all_status$ahead_behind]($style)'

[nix_shell]
symbol = " "
style = "bg:#86BBD8"
format = '[$symbol]($style)'

[cmd_duration]
min_time = 3000
style = "bg:#86BBD8"
format = '[ $duration]($style)'

[line_break]
disabled = false

[character]
success_symbol = "[>](bold green)"
error_symbol = "[>](bold red)"
